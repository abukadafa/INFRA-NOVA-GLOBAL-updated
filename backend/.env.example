# Server Configuration
PORT=5000
NODE_ENV=development

# Database (MongoDB connection string)
# If MongoDB is not reachable, the system automatically falls back to JSON file storage.
MONGODB_URI=mongodb://127.0.0.1:27017/infranova

# JWT Security Key
JWT_SECRET=infranovaglobalconceptsecretkey2026

# Default Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=infranova2026
